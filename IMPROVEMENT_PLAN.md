# Improvement Plan (moved)

This document has been consolidated into the authoritative product spec.

- Product overview, current state, known limitations, and the roadmap now live in [`spec.md`](./spec.md).
- Engineering conventions and agent rules live in [`AGENTS.md`](./AGENTS.md).

The previously "completed" items from this file are reflected in the current
codebase; the remaining ideas were folded into the roadmap and known-limitations
sections of `spec.md`. Do not add new planning content here — update `spec.md` instead.
